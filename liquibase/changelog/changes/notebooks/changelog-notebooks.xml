<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

    <!--
        Notebooks domain changelog. Owned by the notebooks module.
        Historical baseline (0001-initial.xml, 0002-users-notebooks.xml)
        lives in changes/ root; do not re-include it here.

        Cross-schema FK to users.users is intentionally absent so the
        notebooks domain can later move to a NoSQL store without DB-level
        coupling (see api/docs/domain-boundaries.md §4).
    -->

    <include file="0003-move-notebooks-schema.xml" relativeToChangelogFile="true"/>

</databaseChangeLog>
