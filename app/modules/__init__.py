"""Domain modules.

Each subpackage is a self-contained module with its own router, services and
schemas, in line with the multi-module architecture: every module owns its area
of responsibility (HTTP routes, business logic, data contracts).
"""
